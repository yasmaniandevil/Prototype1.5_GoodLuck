using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class Manager : MonoBehaviour
{/*
    public Audio audioController;

    private int currentIndex = 0;
    //script Bopit make a list of these scripts
    //list is in inspector
    //add the cubes that contain the bopit scripts
    public List<Bopit> AudioList = new List<Bopit>();
    public List<Bopit> playerInput = new List<Bopit>();
    
    public GameManager instance; 
    // Start is called before the first frame update

    
    private void Awake()
    {
        if (instance == null)
        {
            DontDestroyOnLoad(gameObject);
            instance = this;
        }
        else
        {
            Destroy(gameObject);
        }
        
        
    }
    public void CheckPlayerInput(Bopit cube)
    {
        if (currentIndex < audioController.sequenceToMatch.Count)
        {
            if (cube == audioController.sequenceToMatch[currentIndex])
            {
               playerInput.Add(cube);
               currentIndex++;

               if (currentIndex == audioController.sequenceToMatch.Count)
               {
                   Debug.Log("Player Matched Sequence");
               }
            }
            else
            {
                Debug.Log("Player clicked wrong cube");
            }
        }
    }
    
  

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
